'use client'
import { useEffect, useState } from 'react'
import { adminApi, Feedback, Module, SaveButton, Status, useAdminRows, useSave } from './admin-module-ui'
const blank = { name: '', colour_hex: '#a98152', description: '', total_sessions: 10, validity_days: 365, price_hkd: 0, serviceIds: [] }
export default function PackagesModule() { const resource = useAdminRows('/api/admin/packages', 'packages'); const save = useSave(); const [services, setServices] = useState([]); const [draft, setDraft] = useState(blank); const [editing, setEditing] = useState(null); useEffect(() => { adminApi('/api/admin/services').then(data => setServices(data.services || [])).catch(() => setServices([])) }, [])
  const submit = async e => { e.preventDefault(); const url = editing ? `/api/admin/packages/${editing}` : '/api/admin/packages'; const ok = await save.submit(() => adminApi(url, { method: editing ? 'PATCH' : 'POST', body: JSON.stringify(draft) })); if (ok) { setDraft(blank); setEditing(null); resource.load() } }
  return <Module title="套票管理" intro="以 HK$ 管理套票及可兌換服務。"><form className="admin-inline-form" onSubmit={submit}><input aria-label="套票名稱" value={draft.name} onChange={e => setDraft({ ...draft, name: e.target.value })} required/><input aria-label="HK$ 售價" type="number" min="0" value={draft.price_hkd} onChange={e => setDraft({ ...draft, price_hkd: Number(e.target.value) })}/><input aria-label="次數" type="number" min="1" value={draft.total_sessions} onChange={e => setDraft({ ...draft, total_sessions: Number(e.target.value) })}/><fieldset><legend>合資格服務</legend>{services.map(service => <label key={service.id}><input type="checkbox" checked={draft.serviceIds.includes(service.id)} onChange={e => setDraft({ ...draft, serviceIds: e.target.checked ? [...draft.serviceIds, service.id] : draft.serviceIds.filter(id => id !== service.id) })}/>{service.name}</label>)}</fieldset><SaveButton pending={save.pending}>{editing ? '儲存更改' : '新增套票'}</SaveButton></form><Feedback {...save}/><Status {...resource}><div className="admin-list">{resource.rows.map(pkg => <article key={pkg.id}><div><strong>{pkg.name}</strong><p>HK${Number(pkg.price_hkd).toFixed(0)} · {pkg.total_sessions} 次</p></div><button type="button" onClick={() => { setEditing(pkg.id); setDraft({ ...blank, ...pkg, serviceIds: pkg.package_services?.map(link => link.service_id) || [] }) }}>編輯</button></article>)}</div></Status></Module> }
